I deployed Jenkins on an Amazon EC2 instance, used a Security Group to control access to Jenkins and administrative connectivity, and attached an IAM role with the permissions Jenkins needs to securely interact with AWS services without storing long-term AWS credentials.